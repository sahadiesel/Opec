{
  "entities": {
    "User": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "User",
      "type": "object",
      "description": "Represents a staff user with access to the application, distinct from workers. Authentication data is managed externally by Firebase Auth.",
      "properties": {
        "id": { "type": "string" },
        "email": { "type": "string", "format": "email" },
        "displayName": { "type": "string" },
        "roleId": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" },
        "updatedAt": { "type": "string", "format": "date-time" }
      }
    },
    "Quotation": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Quotation",
      "type": "object",
      "description": "Commercial proposal document sent to customers prior to PO or Contract.",
      "properties": {
        "id": { "type": "string" },
        "quotationNo": { "type": "string" },
        "customerId": { "type": "string" },
        "title": { "type": "string" },
        "totalAmount": { "type": "number" },
        "status": { "type": "string", "enum": ["DRAFT", "SENT", "ACCEPTED", "REJECTED", "EXPIRED", "CANCELLED"] },
        "issueDate": { "type": "string", "format": "date" },
        "expiryDate": { "type": "string", "format": "date" }
      },
      "required": ["id", "quotationNo", "customerId", "title"]
    },
    "SalesContractTerm": {
      "title": "SalesContractTerm",
      "type": "object",
      "description": "Detailed billing and commercial rules linked to a PO or project.",
      "properties": {
        "id": { "type": "string" },
        "contractNo": { "type": "string" },
        "customerId": { "type": "string" },
        "purchaseOrderId": { "type": "string" },
        "billingCycle": { "type": "string" },
        "vatPercent": { "type": "number" },
        "withholdingTaxPercent": { "type": "number" },
        "status": { "type": "string" }
      }
    },
    "LaborCostContractTerm": {
      "title": "LaborCostContractTerm",
      "type": "object",
      "description": "Detailed cost and payroll rules for specific project deployments.",
      "properties": {
        "id": { "type": "string" },
        "title": { "type": "string" },
        "relatedPurchaseOrderId": { "type": "string" },
        "scopeType": { "type": "string" },
        "status": { "type": "string" }
      }
    },
    "RateCondition": {
      "title": "RateCondition",
      "type": "object",
      "description": "Structured rule for calculating billable revenue or payable labor cost based on events.",
      "properties": {
        "id": { "type": "string" },
        "parentType": { "type": "string", "enum": ["SALES_CONTRACT", "LABOR_COST_CONTRACT", "PO_SNAPSHOT", "WAVE_SNAPSHOT"] },
        "parentId": { "type": "string" },
        "appliesTo": { "type": "string", "enum": ["SALES", "COST"] },
        "eventType": { "type": "string" },
        "calculationMethod": { "type": "string", "enum": ["FLAT", "MULTIPLIER", "PERCENTAGE", "FORMULA"] },
        "unitType": { "type": "string", "enum": ["DAY", "HALF_DAY", "HOUR", "TRIP", "FIXED"] },
        "isActive": { "type": "boolean" }
      },
      "required": ["id", "parentType", "parentId", "appliesTo", "eventType", "calculationMethod", "unitType"]
    },
    "PurchaseOrderProfitSnapshot": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "PurchaseOrderProfitSnapshot",
      "type": "object",
      "description": "Commercial snapshot of estimated profitability for a PO or Wave within a specific period.",
      "properties": {
        "id": { "type": "string" },
        "purchaseOrderId": { "type": "string" },
        "waveId": { "type": "string" },
        "periodStartDate": { "type": "string", "format": "date" },
        "periodEndDate": { "type": "string", "format": "date" },
        "estimatedRevenue": { "type": "number" },
        "estimatedLaborCost": { "type": "number" },
        "estimatedGrossProfit": { "type": "number" },
        "estimatedGrossMarginPercent": { "type": "number" },
        "calculationBasisSummary": { "type": "string" },
        "generatedAt": { "type": "number" },
        "generatedBy": { "type": "string" }
      },
      "required": ["id", "purchaseOrderId", "periodStartDate", "periodEndDate", "estimatedRevenue", "estimatedLaborCost", "generatedAt"]
    },
    "AuditLog": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "AuditLog",
      "type": "object",
      "description": "Detailed record of high-risk actions and data changes for security and compliance.",
      "properties": {
        "id": { "type": "string" },
        "actionType": { "type": "string" },
        "entityType": { "type": "string" },
        "entityId": { "type": "string" },
        "actorUserId": { "type": "string" },
        "actorName": { "type": "string" },
        "eventAt": { "type": "number" }
      },
      "required": ["id", "actionType", "entityType", "entityId", "actorUserId", "eventAt"]
    },
    "NumberSequence": {
      "title": "NumberSequence",
      "type": "object",
      "description": "Configuration and state for automatic document numbering sequences (e.g., BN-2024-0001).",
      "properties": {
        "id": { "type": "string" },
        "sequenceKey": { "type": "string" },
        "label": { "type": "string" },
        "prefix": { "type": "string" },
        "department": { "type": "string" },
        "entityType": { "type": "string" },
        "resetPolicy": { "type": "string", "enum": ["none", "yearly", "monthly"] },
        "year": { "type": "number" },
        "month": { "type": "number" },
        "paddingLength": { "type": "number" },
        "lastNumber": { "type": "number" },
        "lastIssuedCode": { "type": "string" },
        "isActive": { "type": "boolean" },
        "updatedAt": { "type": "number" },
        "updatedBy": { "type": "string" }
      },
      "required": ["id", "sequenceKey", "prefix", "lastNumber", "updatedAt", "isActive"]
    }
  },
  "auth": {
    "providers": ["password", "anonymous"]
  },
  "firestore": {
    "structure": [
      {
        "path": "/quotations/{quoId}",
        "definition": {
          "entityName": "Quotation",
          "schema": { "$ref": "#/backend/entities/Quotation" },
          "description": "Stores sales quotations sent to customers.",
          "params": [{ "name": "quoId", "description": "Unique ID of the quotation." }]
        }
      },
      {
        "path": "/sales_contract_terms/{termId}",
        "definition": {
          "entityName": "SalesContractTerm",
          "schema": { "$ref": "#/backend/entities/SalesContractTerm" },
          "description": "Stores project-specific billing rules.",
          "params": [{ "name": "termId", "description": "Unique ID of the sales term record." }]
        }
      },
      {
        "path": "/labor_cost_contract_terms/{termId}",
        "definition": {
          "entityName": "LaborCostContractTerm",
          "schema": { "$ref": "#/backend/entities/LaborCostContractTerm" },
          "description": "Stores project-specific cost/payroll rules.",
          "params": [{ "name": "termId", "description": "Unique ID of the cost term record." }]
        }
      },
      {
        "path": "/rate_conditions/{conditionId}",
        "definition": {
          "entityName": "RateCondition",
          "schema": { "$ref": "#/backend/entities/RateCondition" },
          "description": "Specific pricing and cost rules for contracts and POs.",
          "params": [{ "name": "conditionId", "description": "Unique ID of the rate condition." }]
        }
      },
      {
        "path": "/po_profit_snapshots/{snapshotId}",
        "definition": {
          "entityName": "PurchaseOrderProfitSnapshot",
          "schema": { "$ref": "#/backend/entities/PurchaseOrderProfitSnapshot" },
          "description": "Stores estimated profitability analyses for POs and Waves.",
          "params": [{ "name": "snapshotId", "description": "Unique ID of the snapshot." }]
        }
      },
      {
        "path": "/audit_logs/{logId}",
        "definition": {
          "entityName": "AuditLog",
          "schema": { "$ref": "#/backend/entities/AuditLog" },
          "description": "Central collection for system-wide audit trails.",
          "params": [{ "name": "logId", "description": "Unique ID of the audit entry." }]
        }
      },
      {
        "path": "/number_sequences/{seqId}",
        "definition": {
          "entityName": "NumberSequence",
          "schema": { "$ref": "#/backend/entities/NumberSequence" },
          "description": "State for automatic document numbering.",
          "params": [{ "name": "seqId", "description": "Key identifying the specific numbering sequence." }]
        }
      }
    ]
  }
}
